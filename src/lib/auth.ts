// TODO(Task 4): Replace with Better Auth GitHub OAuth setup.
// This stub exists so the project typechecks while db.ts (old SQLite) is
// removed but before the Drizzle/Neon + GitHub OAuth wiring is added.
export const auth = {} as Record<string, never>;
